{
  "name": "@possible_triangle/tunnel-server",
  "module": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --outdir ./dist --target bun --sourcemap=linked"
  },
  "dependencies": {
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@possible_triangle/tunnel-contract": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}