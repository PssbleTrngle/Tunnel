{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "port": {
      "description": "which port to forward too",
      "type": "number"
    },
    "host": {
      "description": "which host to forward too",
      "type": "string"
    },
    "target": {
      "description": "host of the tunnel server",
      "type": "string"
    }
  },
  "additionalProperties": false
}